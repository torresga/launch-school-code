each_with_object returns the given object, which in this case would be a hash (updated with all the values from the block)

the arguments for the block are the element, and the new object.

value[0] is the first letter of each element

{'a': 'ant', 'b': 'bear', 'c': 'cat'}
