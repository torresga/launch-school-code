The return value of the following statement is an integer (11), which is the size of the last element ('caterpillar') of the array.

Array#pop destructively removes and returns the last element of an array. Size is called on the returned element, which returns an integer. 
