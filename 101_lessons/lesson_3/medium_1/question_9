"paper"

The function rps uses the results of each of its calls as inputs to the call outside of it.

Start: rps(rps(rps("rock", "paper"), rps("rock", "scissors")), "rock")

rps("rock", "scissors") returns "rock"

rps("rock", "paper") returns "paper"

which becomes:

rps(rps("paper", "rock"), "rock")

rps("paper", "rock") returns "paper"

which becomes:

rps("paper", "rock")

which becomes "paper"
