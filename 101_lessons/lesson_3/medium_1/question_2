We get a TypeError because you cannot concatenate a string and a number together.

Two solutions:

Add to_s: puts "The value of 40 + 2 is " + (40 + 2).to_s
Use string interpolation: "The value of 40 + 2 is #{40 + 2}"
