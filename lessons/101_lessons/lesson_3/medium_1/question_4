The difference is that in the first implementation, she is mutating the buffer array argument with the << method.

In the second implementation, she is assigning a variable named buffer to the addition of two arguments, without mutating the original arguments.
